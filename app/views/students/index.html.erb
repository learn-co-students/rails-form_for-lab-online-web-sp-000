<div>
  <% @students.each do |student| %>
    <div><%= link_to "#{student.first_name} #{student.last_name}", student_path(student) %></div>
  <% end %>
</div

<div><%= link_to "New Student", new_student_path %></div>