<h1>Edit Student Form</h1>

<%= form_for(@student) do |f|  %>
  <%= f.label :first_name %><br>
  <%= f.text_field :first_name, @student.first_name %><br>

  <%= f.label :last_name %><br>
  <%= f.text_field :last_name, @student.last_name %><br>

  <%= f.submit "Update Student" %>
<% end %>
