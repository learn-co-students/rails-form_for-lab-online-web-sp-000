<%= form_for @student do |c| %>
    <%= c.label 'First Name' %><br>
    <%= c.text_field :first_name %><br>

    <%= c.label 'Last Name' %><br>
    <%= c.number_field :last_name %><br>
    <%= c.submit %>
<% end %>